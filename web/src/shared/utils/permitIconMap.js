import { ShopOutlined } from '@ant-design/icons'

/**
 * Icon mapping for permit types
 * Currently using a single icon for all permit types
 */
export function getIconForForm(_formId) {
  return ShopOutlined
}
