# Web Frontend Development Environment
# This file is COMMITTED to git. ./start.sh auto-copies to .env if needed.

VITE_USE_MICROSERVICES=true
VITE_BACKEND_ORIGIN=http://localhost:3001
VITE_CHECK_MAINTENANCE=true
VITE_DISABLE_SOCKET=true
# VITE_TURNSTILE_SITE_KEY=0x4AAAAAACj8RoBha3o812BO

# Session timeout in hours (frontend UI inactivity timer)
VITE_SESSION_TIMEOUT_HOURS=4

# WebAuthn HTTPS configuration
# Set to ngrok URL when using --ngrok flag, otherwise uses localhost
# VITE_WEBAUTHN_ORIGIN=http://localhost:5173

# Dev prefill emails (must have VITE_ prefix for Vite to expose to frontend)
VITE_DEV_EMAIL_ADMIN=froscodiax17912@gmail.com
VITE_DEV_EMAIL_OFFICER=devenecia.stephen@gmail.com
VITE_DEV_EMAIL_OFFICER2=officer2@example.com
VITE_DEV_EMAIL_INSPECTOR=inspector@example.com
